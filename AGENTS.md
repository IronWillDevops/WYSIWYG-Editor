# Project

Modern WYSIWYG editor.

## Rules

Never break public API.

Do not rename exported classes.

Use TypeScript.

No jQuery.

No dependencies.

Support Chrome Firefox Safari Edge.

Every feature must include tests.

Never change formatting configuration.

Never touch package.json unless requested.
